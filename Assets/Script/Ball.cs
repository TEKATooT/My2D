using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ball : MonoBehaviour
{
    //private Camera _camera;
    //private Ball _ball;

    //private float _ballSpeed = 0.1f;

    //public void DirectionToGo(Ball ball)
    //{
    //    StartCoroutine(Go(ball));
    //}

    //private IEnumerator Go(Ball ball)
    //{
    //        _ball.transform.position = Vector2.MoveTowards(_ball.transform.position, _camera.transform.position, _ballSpeed);

    //        yield return null;
    //}
}
